
/**
 *  @jest-environment node
 */

test('Set Readthrough Stop Codon', async () => {

  // add transcript

  // verify transcript

  // parse JSON to get CDS uniquename

  // set readThrough stop codon server

  // get sequence


})


beforeAll(() => {

  // 1. add User
  // 2. add organism directory

  return true
})

afterAll(() => {

  return true
})

