language: node_js

git:
  depth: false

node_js:
  - 8
  - 10
  - 12

script:
  - yarn tsc --noEmit
  - yarn run eslint src
  - yarn test:nowatch
#  - npm run build

cache:
  directories:
    - node_modules
    - yarn
