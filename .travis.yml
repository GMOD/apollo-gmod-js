language: node_js

git:
  depth: false

node_js:
  - 8
  - 10
  - 12

script:
  - npm run-script lint
  - yarn tsc --noEmit
  - yarn test:nowatch
#  - npm run build

cache:
  directories:
    - node_modules
    - yarn
